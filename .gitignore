# ── 数据，不入库 ─────────────────────────────────────────
# 代码和数据已经分开住：数据一律在 data/ 下，整个目录挡掉就够了，
# 不必再逐个文件类型去堵——以后多出什么数据文件都不会漏网。
# 知识库数据可以从原始文档重建，体积也远超 Git 的合理范围。
# 备份请用文件复制（见 _backup/），不要指望 Git。
data/
_backup/

# 万一有人在 data/ 之外建了库，这一行兜底。
*.sqlite3
*.sqlite3-wal
*.sqlite3-shm

# ── 运行时 ──────────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/

# ── 编辑器 / 系统 ───────────────────────────────────────
.vscode/
.idea/
Thumbs.db
desktop.ini

# 安装时定下的数据位置和默认库，一机一份，不入库。
.docatlas-local.toml

# 内部测试流程，不发布。
测试流.md

# 改名前的一次性架构评估，里面还写着作者本机的绝对路径。结论该留的都已经
# 落进 docs/ 和 issues/ 了，原件留在本地即可，不发布。
ARCHITECTURE_REVIEW.md
