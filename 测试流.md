# DocAtlas 高强度真实使用测试流

下面内容可直接交给 AI 执行。

## 任务

你是 DocAtlas 的真实使用测试协调者。模拟普通学习者连续使用技术文档，检验：

- 检索、排序、按需抓取和上下文组织；
- 数据集、语言和版本隔离；
- 实体与关系、证据和失败诊断；
- AI 中间层能否把用户问题转换成官方术语和结构化条件；
- 来源 URL 是否真实支持答案。

测试阶段只诊断和记录问题，不修复产品代码。

项目目录：

```text
C:\Users\HUAI\Desktop\DocAtlas
```

## 测试方向与来源

| 方向 | 指定来源 | 目标数据集 |
|---|---|---|
| C++20 | https://cppreference.com/ | `cppreference-2026-07-26` |
| Blender Shader / Texture Nodes | https://docs.blender.org/manual/en/5.2/ | `blender-manual-5.2` |
| Blender Geometry Nodes | https://docs.blender.org/manual/en/5.2/ | `blender-manual-5.2` |
| Roblox Studio 项目 | https://create.roblox.com/docs/studio | `roblox-creator-2026-07-26` |

默认每个方向 12 轮，共 48 轮。环境提供 `ROUNDS_PER_AGENT` 时使用该有限正整数。
不得解释为无限运行。

## 不可违反的边界

1. 工作分为“准备数据集”和“只读测试”两个阶段。
2. 准备阶段只允许补齐来源适配器、数据集配置及其最小测试。
3. 数据集验收后冻结产品代码；测试阶段只允许主智能体维护 `issues/`。
4. 测试智能体只查询、在线核对并返回记录，不修改文件、不分配 Issue 编号、不提交、
   不推送、不创建 PR，也不得继续创建子智能体。
5. 主智能体负责数据集准备、关键复现、Issue 审核、编号、索引和校验。
6. 不直接读取或扫描大型数据库、清单和导出文件；使用 DocAtlas 正常入口。
7. 开始前检查 Git，保留用户已有工作，不覆盖或清理无关改动。
8. 本轮新增或更新的数据集数据库必须保留。只有数据库属于强制保留范围；其他临时
   产物只有在确认是本轮新建且用户未要求保留时才可清理。
9. 所有本地操作使用 PowerShell。
10. 未经用户明确授权，不提交、推送或创建 PR。

## 阶段 0：开始前检查

主智能体完整读取：

- 已安装的 DocAtlas `SKILL.md` 与 `WORKFLOWS.md`；
- `issues/README.md`、`issues/WORKFLOW.md` 和两份模板；
- 适用的 `AGENTS.md`。

随后记录：

1. 当前分支和 Git 状态。
2. `python -m docatlas paths`。
3. 四个目标数据集的配置、目录、版本、语言和数据库状态。
4. 各库的 inventory/content 验收结果。
5. 是否配置领域知识包，以及实际声明的关系和证据类型。

不得只凭目录存在就认定数据集可用。

## 阶段 1：准备数据集

已有且验收通过的库直接复用；验收失败先诊断；不存在时才建立。通用核心缺陷阻断
建库时记录为 `setup_blocker`，不要临时修改核心绕过。

每个需要建立或补齐的数据集依次执行：

```powershell
$env:DOCATLAS_DATASET='<dataset_id>'
python -m docatlas crawl --discovery-only
python -m docatlas validate --phase inventory
python -m docatlas crawl --skip-discovery --sample-per-category 20
python -m docatlas validate --phase content
python -m docatlas ask "<明确存在的官方术语>" --token-budget 1500
```

检查实际数字、正文、标题、语言、正式来源 URL、分类、关系和缺页报告。小样通过后
停止，不执行全站正文下载。

### 数据集要求

- cppreference：固定快照，至少区分 `language`、`standard_library`、
  `compiler_support`。C++20 问题传 `version_target="C++20"` 和
  `version_mode="strict"`。
- Blender：固定 5.2，不使用 `latest`。当前数据集确有历史证据时，迁移问题传
  `version_mode="migration"`，不能一律压低旧版本证据；历史证据不在 5.2 来源时
  记录资料覆盖边界，不得判为版本合同 Bug。
- Roblox：持续更新站点的快照日期是数据集身份，不是正文内容版本。每次显式传
  `dataset_id="roblox-creator-2026-07-26"`；当
  `docatlas_list_datasets.version_vocabulary` 为空时，不传 `version_target` 或
  `version_mode="strict"`。至少区分 `studio_guides`、`luau`、`engine_api`、
  `open_cloud`。Studio 项目默认只用前三类；除非问题明确涉及外部 HTTP API，否则
  不得混入 Open Cloud。

Roblox 在线文档可能晚于本地快照。出现差异时先判断是否为来源更新，不能直接判为
DocAtlas Bug。

四个方向对应的数据集均可用后，进入只读测试。

## 阶段 2：建立四个测试方向

为以下四个方向各建立一个只读测试智能体。按环境并发容量分批执行；不得因为并发
槽位不足漏掉任何方向。

### A. C++20 学习

覆盖基础类型与初始化、引用与生命周期、类与继承、RAII、模板与 concepts、
常用容器、算法与 ranges、时间、文件系统、并发和标准差异。

### B. Blender Shader / Texture Nodes

覆盖 Shader Editor、Principled BSDF、坐标与 Mapping、Image Texture、程序纹理、
Color Ramp、Mix/Math、法线与位移、实际材质、节点组、Eevee/Cycles 和版本变化。

### C. Blender Geometry Nodes

覆盖编辑器与 Modifier、Field/Socket/Domain、Position/Index/Normal、几何变换与
转换、实例、属性、程序化散布、道路/围栏、节点组和弃用节点。

### D. Roblox Studio 小项目

以“制作并准备发布一个小型多人收集挑战”为连续项目，覆盖：

1. 安装、更新与 Studio 设置。
2. 界面布局、Explorer、Properties 和项目数据模型。
3. Part、Model、移动/旋转、Align 和 Pivot。
4. Terrain、材质、资源与基础场景搭建。
5. Script Editor、Luau、代码补全和脚本位置。
6. Output、调试器和 Developer Console。
7. 服务、属性、事件以及客户端/服务器职责。
8. 收集物、计分或检查点等交互逻辑。
9. 基础界面与不同设备尺寸适配。
10. Playtest、multi-client simulation 和 device emulation。
11. Experience Settings、发布、权限与协作。
12. 用普通用户的自然表达描述一次综合问题，再由 AI 转成官方术语完成查询。若该
    方向承担全局跨语言测试，可以使用中文提问。

这是文档检索验收，不要求实际启动或操作 Roblox Studio。若环境另行授权桌面操作，
再把真实操作结果作为附加证据。

## MCP 与数据集隔离

开始时调用 `docatlas_list_datasets`，确认四个库的身份、版本、语言、分类和状态。

同一 MCP 可以服务所有库，但每次调用必须显式传 `dataset_id`。不得依赖默认库。
MCP 缺少目标库或不可用时，才回退 CLI：

```powershell
$env:DOCATLAS_DATASET='<dataset_id>'
python -m docatlas ask "<官方术语>" --token-budget 1500
```

每轮记录 MCP 或 CLI，以及实际数据集 ID。连续切换数据集时检查结果是否串库。

## 每轮测试协议

1. 生成一个符合当前学习阶段的真实问题，第一次不得先看在线答案。
2. AI 先识别用户意图、官方术语、分类和版本条件，再调用 `ask`；同时保留原问题和
   实际查询词，不能要求用户自己翻译。只有数据集声明 `version_vocabulary` 时才传
   内容版本条件；快照日期只通过 `dataset_id` 选择。
3. 简单问题预算 1500，一般问题 3000，确需跨页时才用 6000。
4. 结果不足时再用 `search`；只使用本轮返回的 K 编号调用 `show`。
5. 只有问题涉及归属、对应接口或跨页关系时才调用 `related`。
6. 弱结果、`language_mismatch` 或 `candidates_too_weak` 时，由 AI 改写官方术语
   重试，不放宽核心抓取边界。
7. 得到 DocAtlas 结果后才访问指定版本来源核对。
8. 检查首位命中、正文、版本、语言、出处、必要条件、关系证据和失败说明。
9. 首次补抓标记为冷查询；页面本地化后可复查一次，区分网络与本地问题。
10. `related` 若列出同一清单内的安全 pending 路径，AI 用相同 `dataset_id`、
    精确 path 和有限 `fetch_limit` 调用 `ask` 补抓，再重试一次 `related`；
    不要求用户手工拼命令。inventory 外目标、弱候选或失败时停止并记录边界。
11. 返回结构化记录。

冷查询和本地复查属于同一轮。

## 关系测试

所有数据集都测试通用官方链接关系：

- `evidence_kind=official_link`
- `confidence=1.0`
- 实体、方向和 `evidence_url` 与原文一致
- 状态与 `next_steps` 符合实际情况；安全 pending 按每轮协议自动完成一次闭环

只有配置领域知识包时才测试领域关系。`confidence<1.0` 必须表述为推断或候选。
没有知识包时记录“仅通用关系层”，不能把没有领域关系判为 Bug。

每个方向至少包含一次通用关系测试。条件允许时分别验证完整索引关系和按需增量关系。

## 每轮记录

记录 ID：

- `CPP-R01`
- `SHADER-R01`
- `GEOMETRY-R01`
- `ROBLOX-R01`

最少记录：

- 角色、轮次、学习问题；
- 数据集 ID、版本、语言、入口；
- 原问题、AI 生成的官方术语和结构化参数；
- 工具调用、状态、耗时、是否补抓；
- 首位结果、知识块和 DocAtlas 来源 URL；
- 在线核对 URL、核对结果和版本一致性；
- 关系层级、类型、方向、证据、置信度和建立路径；
- 结果分类、问题候选、原始证据和下一步。

原始证据必须包含可复现查询、关键返回字段、耗时、两个来源 URL，以及预期与实际差异。

## 测试强度

每个方向默认 12 轮：

- 4 轮基础概念；
- 4 轮实际任务；
- 2 轮跨页或关系；
- 1 轮 AI 中间层测试：用户先用自然口语或同义表达提问，再由 AI 理解意图，
  转换成数据集原文语言的官方术语和结构化条件后调用 MCP；不要求使用中文；
- 1 轮版本差异或易混淆问题。

轮数变化时按比例调整，但必须保留实际任务、自然问法、关系和版本/混淆场景。

跨语言查询是四个方向共用的验收项，不是每个方向的固定配额：全部测试合计至少
安排 1 轮。该轮可以使用中文提问，检验的是 AI 能否保留技术符号和版本条件，
将问题转换成数据集原文语言的官方术语后调用 MCP；不是要求 DocAtlas 核心直接
理解中文。

## 结果分类

- `confirmed_bug`：崩溃、超时、错误版本、错误来源、正文损坏、确定性错排、关系或
  合同错误。
- `enhancement`：功能可用，但用户需要过多尝试，或诊断、关系、版本表达不清。
- `source_limitation`：指定原文本身没有答案。
- `setup_blocker`：数据集、来源或工具不可用，无法继续。
- `no_issue`：结果正确且证据有效。

一次不理想结果不能直接建立 Issue。除确定性崩溃或数据损坏外，至少完成：

1. 原查询复查；
2. 自然改写；
3. 精确官方术语对照；
4. 区分补抓、清单、检索、排序、解析、裁剪和关系阶段；
5. 冻结版本在线核对；
6. 排除重复 Issue。

测试智能体只报告候选；主智能体必须亲自复现关键问题。

## Issue 维护

写入前读取索引、流程和模板。只有主智能体可以修改 `issues/`。

- 新问题放 `issues/bugs/unresolved/`。
- 新增强放 `issues/enhancements/unresolved/`。
- 一个文件只处理一个可独立关闭的事项。
- 使用下一编号，记录最小复现、实际证据、验证方法和必要边界。
- 参考建议不是强制实现方案。
- 已解决事项只有确认回归时才重新打开，并保留原历史。
- `fix_pr` 在真实 PR 创建前保持 `null`。
- 更新索引后运行 `.\scripts\validate-issues.ps1`。

## 停止条件

单个方向满足任一条件时停止：

- 完成指定轮数；
- 同一阻塞连续出现 3 次；
- 来源或入口持续不可用，继续测试不会产生新证据；
- 主智能体要求停止。

提前停止时报告完成轮数、阻塞轮次、连续次数、替代入口和停止原因。一个方向停止
不影响其他方向。

## 最终报告

只报告实测数字：

- 四个方向各自及总完成轮数；
- MCP/CLI 调用数、冷查询和按需抓取数；
- 成功、失败、超时、中位耗时和最慢查询；
- 正确首位命中数、有效来源数；
- 通用/领域关系测试与通过数；
- 完整索引/按需增量关系测试数；
- 关系实体、方向、证据或置信度错误数；
- 各结果分类数量；
- 新建或更新的 Issue；
- 保留数据库的路径、阶段、验证状态和大小；
- Issue 校验结果和最终 Git 状态。

不得用“应该没问题”等模糊结论。保留数据库；不提交、不推送、不创建 PR。
