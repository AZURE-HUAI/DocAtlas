# DocAtlas 高强度真实使用测试流

下面内容可直接交给 AI 执行。项目目录：`C:\Users\HUAI\Desktop\DocAtlas`。

## 任务

你是 DocAtlas 的真实使用测试协调者。模拟普通学习者连续使用技术文档，检验：

- 检索、排序、按需抓取和上下文组织；
- 数据集、语言和版本隔离；
- 实体与关系、证据和失败诊断；
- AI 中间层能否把用户问题转换成官方术语和结构化条件；
- 来源 URL 是否真实支持答案。

测试阶段只诊断和记录问题，不修复产品代码。

**这次测试的成败不看找到多少问题，看上报的每一条是不是真问题。** 一条查不出
根因的报告，比没有报告更贵：它会占用一轮修复、改动能正常工作的代码，然后在
真实使用里原样复发。宁可零上报，不可上报错的。

---

# 第一部分：什么才算真问题

**先读这一部分再开始测试。** 所有轮次的判定都以它为准。

## 真问题的定义

同时满足三条，缺一不可：

1. **程序可解决** —— 说得出改哪个文件、哪一层的什么行为。
2. **非外部因素** —— 不是官网改版、网络、来源站本身没有这段内容。
3. **非操作不当** —— 按手册规范调用仍然复现。

## 上报前的四道闸门

任何候选问题，**四道全过才能写进 `issues/`**。任一道没过，按该道的判定归类，
不进 `issues/bugs/`。判定过程要写进记录，不能只写结论。

### 闸门 1：规范调用复跑

用规范形式重跑一次：

- 走 MCP，不是 CLI；
- 显式传 `dataset_id`；
- 用数据集**原文语言**的官方术语，不是用户原话；
- 预算按问题复杂度给（1500 / 3000 / 6000），不是一律最小；
- 知道分类就传 `category`。

**规范形式下通过 → 不是 bug。** 这是调用方式问题。真正值得记的是"为什么第一次
没做对"：如果 DocAtlas 的返回里已经给了线索而 AI 没照做，那是 AI 层的执行问题；
如果返回里根本没有可照做的线索，那才是一条**诊断类增强**（`enhancement`），
标题写"某某情况下没有给出可执行的下一步"，不要写成检索 bug。

### 闸门 2：内容到底在不在本地库里

必须先证明"正确答案确实在这个数据集里"，再谈检索错了。

```powershell
$env:DOCATLAS_DATASET='<dataset_id>'
python -m docatlas ask "<你认为正确的那一页>" --json --no-fetch
python -m docatlas search "<官方术语>" --limit 10
```

- 库里根本没有这一页 → **`source_limitation` 或收录范围问题，不是检索 bug。**
  改查询词永远解决不了，要改的是来源适配器或数据集声明的目录
  （见 `WORKFLOWS.md` 流程 B）。
- 清单里有、正文没抓 → 先补抓再复判，**不能拿"没抓"当"找不到"**。

历史教训：Blender 库没声明 `editors/` 目录，于是 `Shader Editor` 查不到。这看着
像检索缺陷，实际是收录范围。当时若直接按检索 bug 上报，会去改排序逻辑，而那一页
根本不在库里，改什么都不会命中。

### 闸门 3：说出要改哪一层

写出一句话：**"修这个问题，要改 `<文件>` 的 `<行为>`。"** 写不出来就不是真问题。

以下几类写不出来，直接判定不是 bug：

| 现象 | 为什么不是 bug |
|---|---|
| "核心应该看懂中文提问" | 语言转换是 AI 中间层职责，核心只做确定性检索 |
| "核心应该自己猜用户要哪个版本" | 版本意图由调用方判断后传入，核心不猜 |
| "核心应该知道这是概念题还是 API 题" | 已按查询形状做了区分；要它读心不是可实现的行为 |
| "用户还得再敲一条命令" | 那条命令是给 AI 执行的，不是给用户的 —— 见下 |

**ENH-010 就是栽在最后一条。** 测试报告说"`related` 返回 pending 目标后，用户
必须自己拼一条 `ask` 才能拿到关系"，作为增强提了上来。审查后发现底层能力一点
没缺：返回里已经给出精确路径和下一步，抓完关系也能正确建立。缺的是 **AI 没有
去执行那一步**。最后这条被 `closed`，改成了 SKILL.md 里的一条编排规则。

所以判定 AI 编排类问题之前，先自查：**SKILL.md 要求 AI 做的六件事，这一轮做了
吗？** 没做而失败的，是本次测试的执行问题，不是 DocAtlas 的问题。

### 闸门 4：在用户真正用的那一层复现

复现必须走完整的 `docatlas_ask`（或 CLI `ask`），**不能只测内部函数**。

以下全都**不等于**通过：定位器认出了 URL、页面抓取成功、目标页出现在候选里、
目标页排第二。只有"最终回答的正文和出处来自正确页面"才算通过。

历史教训：BUG-008 第一次修复只验证了"候选定位器能认出 URL"，测试全绿，而用户
实际拿到的回答仍然是错的页面——因为回答层没用上定位结果。**辅助函数通过而最终
输出失败，仍然是产品问题；反过来，只测辅助函数得出的"通过"不作数。**

## 一条必须记住的事实：`status: ok` 不代表答对了

实测：向 Blender 库查一串乱码 `zzqqxx nonexistent thing`，返回
`status: ok` 加 1 个知识块。`ok` 的含义只是"至少有一块内容"，不是"这块内容
回答了你的问题"。

**所以判定合格与否，看这三样，不看 `status`：**

1. 首位结果是不是正确那一页；
2. 进入回答的**所有**页面是不是都相关；
3. 出处 URL 打开后是不是真的支持这段回答。

`status` 只用来区分"哪一种没有"，不用来判断"答得对不对"。

## 判定表

| 观察到的 | 归类 | 写不写 issue |
|---|---|---|
| 崩溃、超时、正文损坏、串库 | `confirmed_bug` | 写，高优先级 |
| 四闸全过，首位稳定错、出处不支持回答 | `confirmed_bug` | 写 |
| 四闸全过，答案对但要绕很多弯 | `enhancement` | 写 |
| 返回没给出可执行的下一步 | `enhancement`（诊断类） | 写 |
| 规范调用下通过 | 操作问题 | **不写**，记进本轮记录 |
| 正确内容不在本库 | `source_limitation` / 收录范围 | **不写 bug**；确需扩范围写 `enhancement` |
| 要求核心猜语言、版本或意图 | 设计如此 | **不写** |
| AI 没执行 `next_steps` 导致的失败 | 本次执行问题 | **不写** |
| 官网内容比本地快照新 | 来源漂移 | **不写**，记录快照边界 |
| 数据集或工具起不来 | `setup_blocker` | 不写 issue，立即上报主智能体 |

## 复现强度

除确定性崩溃或数据损坏外，候选问题至少完成：原查询复查 → 自然改写 → 精确官方
术语对照 → 区分是补抓/清单/检索/排序/解析/裁剪/关系哪个阶段 → 冻结版本在线核对
→ 排除重复 issue。

测试子智能体**只报告候选**；主智能体必须亲自过四道闸门并复现，才可写入 `issues/`。

---

# 第二部分：怎么测

## 不可违反的边界

1. 工作分"准备数据集"和"只读测试"两个阶段；数据集验收后冻结产品代码。
2. 准备阶段只允许补齐来源适配器、数据集配置及其最小测试。
3. 测试子智能体只查询、在线核对、返回记录：不改文件、不分配 Issue 编号、
   不再创建子智能体。只有主智能体可以维护 `issues/`。
4. 不直接读取或扫描大型数据库、清单、导出文件；一律走 DocAtlas 正常入口。
5. 开始前检查 Git，保留用户已有工作，不覆盖无关改动。
6. 本轮新增或更新的数据集数据库必须保留。仓库里已提交的正式回归测试是产品资产，
   不得删除或回退。
7. 所有本地操作使用 PowerShell。
8. **未经用户明确授权，不提交、不推送、不创建 PR。**
9. 主智能体和每个子智能体都必须**亲自完整读取**适用的 Skill，不得用摘要、旧对话
   记忆或转述代替。读取失败即 `setup_blocker`。

## 执行顺序

前一步没过就不进下一步。

1. 同步 Codex 侧 Skill，确认 MCP 指向当前项目且五个工具可用。
2. 主智能体读维护说明，核对 Git、四个数据集、MCP 五工具。
3. 主智能体为每个方向发出完整、独立的子智能体任务。
4. 子智能体自行完整读取已安装 `SKILL.md`，过启动门槛后才开测。
5. 主智能体过四道闸门复现候选，只把确认后的写入 `issues/`。
6. 汇总实测数字，核对 Git。

## 阶段 0：开始前检查

```powershell
python install.py
```

安装成功只说明文件到位。**还必须用 Codex 配置里的实际 MCP 启动方式拉起新进程**，
完成 `initialize`、`tools/list` 和一次 `docatlas_list_datasets`，确认这五个工具
全部可见可调用：`docatlas_ask`、`docatlas_search`、`docatlas_show`、
`docatlas_related`、`docatlas_list_datasets`。

缺任一工具、调用失败、仍指向旧项目 → `setup_blocker` 并停止。**不能因为 CLI 能用
就声称 MCP 已同步。** 当前任务可能缓存了旧工具清单，需新开任务或重连后再验收。

随后完整读取：已安装的 `SKILL.md` 与 `WORKFLOWS.md`、`issues/README.md`、
`issues/WORKFLOW.md`、两份模板、适用的 `AGENTS.md`。

然后记录：当前分支与 Git 状态；`python -m docatlas paths`；四个数据集的配置、
目录、版本、语言、数据库状态；各库 inventory/content 验收结果；是否配置领域知识包
及其实际声明的关系与证据类型。

**不得只凭目录存在就认定数据集可用。**

## 阶段 1：准备数据集

已有且验收通过的库直接复用；验收失败先诊断；不存在时才建立。通用核心缺陷阻断
建库时记为 `setup_blocker`，**不要临时改核心绕过**。

```powershell
$env:DOCATLAS_DATASET='<dataset_id>'
python -m docatlas crawl --discovery-only
python -m docatlas validate --phase inventory
python -m docatlas crawl --skip-discovery --sample-per-category 20
python -m docatlas validate --phase content
python -m docatlas ask "<明确存在的官方术语>" --token-budget 1500
```

检查实际数字、正文、标题、语言、正式来源 URL、分类、关系和缺页报告。小样通过后
停止，**不执行全站正文下载**。

### 各数据集的既定条件

- **cppreference**：固定快照，至少区分 `language`、`standard_library`、
  `compiler_support`。C++20 问题传 `version_target="C++20"` 与
  `version_mode="strict"`。
- **Blender**：固定 5.2，不使用 `latest`。确有历史证据时迁移问题传
  `version_mode="migration"`；历史证据不在 5.2 来源时记录**资料覆盖边界**，
  不得判为版本合同 Bug。
- **Roblox**：快照日期是**数据集身份**，不是正文内容版本。每次显式传
  `dataset_id="roblox-creator-2026-07-26"`；`version_vocabulary` 为空时不传
  `version_target`，也不传 `version_mode="strict"`。至少区分 `studio_guides`、
  `luau`、`engine_api`、`open_cloud`；Studio 项目默认只用前三类，除非问题明确
  涉及外部 HTTP API，否则不混入 Open Cloud。在线文档可能晚于本地快照，
  **差异先判断是不是来源更新**（闸门 2），不能直接判为 DocAtlas Bug。

四个方向的数据集均可用后，进入只读测试。

## 阶段 2：四个测试方向

| 方向 | 指定来源 | 目标数据集 |
|---|---|---|
| C++20 | https://cppreference.com/ | `cppreference-2026-07-26` |
| Blender Shader / Texture Nodes | https://docs.blender.org/manual/en/5.2/ | `blender-manual-5.2` |
| Blender Geometry Nodes | https://docs.blender.org/manual/en/5.2/ | `blender-manual-5.2` |
| Roblox Studio 项目 | https://create.roblox.com/docs/studio | `roblox-creator-2026-07-26` |

各建一个只读测试智能体。按并发容量分批，**不得因槽位不足漏掉任何方向**。

### 子智能体启动门槛

主智能体发给子智能体的任务必须**包含下面整段**，不能只说"按测试流执行"：

```text
你的第一步必须是完整读取：
C:\Users\HUAI\.codex\skills\docatlas\SKILL.md

不得依赖主智能体的摘要或旧对话记忆。读取失败时立即停止，返回 setup_blocker。
读取完成后先报告：
1. 实际读取的文件路径；
2. 本方向的 dataset_id；
3. 将遵守的五条查询规则：MCP 优先、显式 dataset_id、AI 负责术语转换、
   精确 URL/path 直接定位、按 status/next_steps 完成有限闭环。

另外确认你已理解：status=ok 不代表答案正确，判定看首位命中、进入回答的所有
页面、出处 URL 是否真的支持回答；你只报告问题候选，不做最终判定，不写 issue。

完成以上启动回执后才能开始第 1 轮。你只读测试，不修改任何文件，不创建子智能体，
不分配 Issue 编号，不提交，不推送。
```

子智能体若还承担建库、升级、重加工、体检或适配器准备，须完整读取 `WORKFLOWS.md`。
只读查询的子智能体不必把维护流程全文重复进报告。

每个子任务还须写明：方向、目标数据集 ID、轮数、允许的来源版本、只读边界、停止
条件、返回记录格式。**不得假设子智能体能看到主智能体的上下文。**

### 各方向覆盖面

**A. C++20 学习** —— 基础类型与初始化、引用与生命周期、类与继承、RAII、模板与
concepts、常用容器、算法与 ranges、时间、文件系统、并发、标准差异。

**B. Blender Shader / Texture Nodes** —— Shader Editor、Principled BSDF、坐标与
Mapping、Image Texture、程序纹理、Color Ramp、Mix/Math、法线与位移、实际材质、
节点组、Eevee/Cycles、版本变化。

**C. Blender Geometry Nodes** —— 编辑器与 Modifier、Field/Socket/Domain、
Position/Index/Normal、几何变换与转换、实例、属性、程序化散布、道路/围栏、
节点组、弃用节点。

**D. Roblox Studio 小项目** —— 以"制作并准备发布一个小型多人收集挑战"为连续项目：
安装与 Studio 设置 → 界面布局与数据模型 → Part/Model/Align/Pivot → Terrain 与
场景搭建 → Script Editor 与 Luau → Output 与调试器 → 服务/属性/事件与客户端
服务器职责 → 收集物与计分逻辑 → 界面与设备适配 → Playtest 与 device emulation
→ Experience Settings 与发布 → 用普通用户的自然表达描述一次综合问题，再由 AI
转成官方术语完成查询。

这是**文档检索验收**，不要求实际启动或操作 Roblox Studio。若另行授权桌面操作，
再把真实操作结果作为附加证据。

## MCP 与数据集隔离

开始时调用 `docatlas_list_datasets`，确认四个库的身份、版本、语言、分类和状态。

同一 MCP 服务所有库，但**每次调用必须显式传 `dataset_id`**，不得依赖默认库。
MCP 缺少目标库或不可用时才回退 CLI，且必须明确记录为回退——**CLI 结果不算 MCP
通过**。每个子智能体在自己第 1 轮前要再次调用 `docatlas_list_datasets`，确认它
自己拿到了 MCP，而不是继承主智能体的结论。

连续切换数据集时检查结果是否串库。

**精确页面合同**：四个方向各至少一次，把已知官方 URL 或返回的 inventory path
原样交给 `docatlas_ask`，确认最终答案**只来自被指名页面**。目标页仅出现在候选、
排第二第三、或仅显示"已抓取"，一律判失败。

## 每轮测试协议

1. 生成一个符合当前学习阶段的真实问题，**第一次不得先看在线答案**。
2. AI 先识别意图、官方术语、分类和版本条件，再调用 `ask`；保留原问题与实际查询词，
   **不能要求用户自己翻译**。只有数据集声明 `version_vocabulary` 时才传内容版本
   条件；快照日期只通过 `dataset_id` 选择。
3. 预算：简单 1500，一般 3000，确需跨页才 6000。
4. 结果不足时再用 `search`；只用本轮返回的 K 编号调 `show`。
5. 只有涉及归属、对应接口或跨页关系时才调 `related`。
6. 弱结果、`language_mismatch`、`candidates_too_weak` 时由 AI 改写官方术语重试，
   **不放宽核心抓取边界**。
7. 得到 DocAtlas 结果**之后**才访问指定版本来源核对。
8. 检查首位命中、进入回答的所有页面、正文、版本、语言、出处、必要条件、关系证据
   和失败说明。
9. 首次补抓记为冷查询；页面本地化后可复查一次，区分网络问题与本地问题。冷查询与
   本地复查属于**同一轮**。
10. `related` 若列出同一清单内的安全 pending 路径，**AI 自己**用相同 `dataset_id`、
    精确 path 和有限 `fetch_limit` 调 `ask` 补抓，再重试一次 `related`。
    inventory 外目标、弱候选或失败时停止并记录边界。
    **不要求用户手工拼命令——这一步没做就不算测过。**
11. 判定落在最终输出层（闸门 4）：中间候选正确但最终答案引用了别的页面，
    按排序或回答约束问题记录。
12. 返回结构化记录。

## 关系测试

所有数据集都测通用官方链接关系：`evidence_kind=official_link`、`confidence=1.0`、
实体/方向/`evidence_url` 与原文一致、状态与 `next_steps` 符合实际，安全 pending
按第 10 条自动完成一次闭环。

只有配置领域知识包时才测领域关系。`confidence<1.0` 必须表述为推断或候选。
**没有知识包时记"仅通用关系层"，不能把没有领域关系判为 Bug。**

每个方向至少一次通用关系测试；条件允许时分别验证完整索引关系与按需增量关系。

## 测试强度

每个方向默认 12 轮，共 48 轮。环境提供 `ROUNDS_PER_AGENT` 时用该有限正整数，
**不得解释为无限运行**。

- 4 轮基础概念；
- 4 轮实际任务；
- 2 轮跨页或关系；
- 1 轮 AI 中间层：用户先用自然口语或同义表达提问，再由 AI 转成原文语言的官方术语
  和结构化条件后调用 MCP；
- 1 轮版本差异或易混淆问题。

轮数变化时按比例调整，但必须保留实际任务、自然问法、关系和版本/混淆场景。

**跨语言查询是四方向共用验收项**，合计至少 1 轮，不是每方向固定配额。该轮可用
中文提问，检验的是 AI 能否保留技术符号与版本条件、转换成原文官方术语后调用 MCP，
**不是要求 DocAtlas 核心直接理解中文**。

## 每轮记录

记录 ID：`CPP-R01` / `SHADER-R01` / `GEOMETRY-R01` / `ROBLOX-R01`。

最少记录：角色、轮次、学习问题；数据集 ID、版本、语言、入口；原问题、AI 生成的
官方术语与结构化参数；工具调用、状态、耗时、是否补抓；首位结果、知识块、
DocAtlas 来源 URL；在线核对 URL、核对结果、版本一致性；关系层级、类型、方向、
证据、置信度、建立路径；结果分类、问题候选、原始证据、下一步。

原始证据必须含可复现查询、关键返回字段、耗时、两个来源 URL，以及预期与实际差异。

**报候选问题时，额外写明四道闸门各自的结论与依据**，缺一律退回补测。

## 临时探针与 Git 回退

只读测试原则上不创建代码或测试文件。主智能体确需临时探针时：

1. 先记录测试前的 HEAD、Git 状态和临时改动的精确文件；
2. 优先用一次性 PowerShell 命令或仓库外临时目录；
3. 若改了仓库文件，只回退本次新增的精确改动，不覆盖用户已有改动；
4. 测完用 Git 恢复，并核对工作区、暂存区、未跟踪文件；
5. 已提交的回归测试不是临时探针，不得删除、回退或改写历史。

临时探针通过只证明本次复现。需要长期防复发时，在**修复阶段**另行决定是否加入正式
回归测试，只读测试智能体不得擅改产品代码。

## Issue 维护

写入前读索引、流程和模板。**只有主智能体可以修改 `issues/`，且只写四闸全过的。**

- 新问题放 `issues/bugs/unresolved/`，新增强放 `issues/enhancements/unresolved/`；
- 一个文件只处理一个可独立关闭的事项；
- 用下一编号，记录最小复现、实际证据、验证方法和必要边界；
- **四道闸门的结论写进"复现"一节** —— 后来人据此判断这条是否仍然成立；
- 参考建议不是强制实现方案；
- 已解决事项只有确认回归才重开，保留原历史；
- `fix_pr` 在真实 PR 创建前保持 `null`；
- 更新索引后运行 `.\scripts\validate-issues.ps1`。

## 停止条件

单个方向满足任一条件即停止：完成指定轮数；同一阻塞连续出现 3 次；来源或入口持续
不可用且继续测不会产生新证据；主智能体要求停止。

提前停止时报告完成轮数、阻塞轮次、连续次数、替代入口和停止原因。一个方向停止
不影响其他方向。

## 最终报告

只报实测数字：

- 四方向各自及总完成轮数；
- 四个子智能体的 Skill 实际读取路径与启动门槛结果；
- MCP 五工具可见性、实际调用结果、CLI 回退次数；
- MCP/CLI 调用数、冷查询数、按需抓取数；
- 成功、失败、超时、中位耗时、最慢查询；
- **正确首位命中数、出处真实支持回答的条数**（不是 `status=ok` 的条数）；
- 通用/领域关系测试数与通过数；完整索引/按需增量关系测试数；
  关系实体、方向、证据或置信度错误数；
- 各结果分类数量；
- **候选问题总数，以及各道闸门分别拦下多少条**——这是本次测试有效性的主要指标；
- 新建或更新的 Issue；
- 保留数据库的路径、阶段、验证状态、大小；
- Issue 校验结果与最终 Git 状态。

不得用"应该没问题"等模糊结论。保留数据库；不提交、不推送、不创建 PR。
